apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-green
spec:
  template:
    spec:
      containers:
      - name: app
        envFrom:
        - secretRef:
            name: backend-secret
